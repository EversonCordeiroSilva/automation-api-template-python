# pages

This folder is intentionally left unused.

It exists only to keep the structure open for potential UI automation in the future
(e.g. Page Object Model).

This project currently focuses exclusively on API automation using BDD.
